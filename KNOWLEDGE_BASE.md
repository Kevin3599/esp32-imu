# Embedded Knowledge Base

> 个人技术知识库 · ESP32 车载性能仪项目
> 半年后重新打开这个仓库时，**只读这一份文档**就能想起来：当时做了什么、为什么这么设计、数据怎么流、核心技术是什么、踩过哪些坑。
> 生成日期：2026-06-10 · 工作区：`e:\esp32\esp32-imu`

---

## 目录

- [第一部分：项目全景图](#第一部分项目全景图)
- [第二部分：系统架构设计](#第二部分系统架构设计)
- [第三部分：项目运行流程](#第三部分项目运行流程)
- [第四部分：核心模块解析](#第四部分核心模块解析)
- [第五部分：技术知识提取](#第五部分技术知识提取)
- [第六部分：设计决策分析](#第六部分设计决策分析)
- [第七部分：调试与踩坑记录](#第七部分调试与踩坑记录)
- [第八部分：工程实践总结](#第八部分工程实践总结)
- [第九部分：项目复盘](#第九部分项目复盘)
- [第十部分：个人知识地图](#第十部分个人知识地图)

> **关于工作区范围**：当前工作区只有**一个固件项目**（`esp32-imu`），外加它的随附文档（GPS 升级分析、面试归档、接线图、构建指南）和一个 `cad/` 里的 OpenSCAD 外壳模型 + Gerber 压缩包。本文档据此组织。CAD 外壳是该项目的机械配套件，单独在末尾点到为止。

---

# 第一部分：项目全景图

## 项目名称：ESP32 车载性能仪（"Dragy 风格" Performance Box）

### 项目背景

为什么会有这个项目？因为想用便宜的开源硬件，自己做一台**摩托车/汽车的车载性能记录仪**，对标商业产品 Dragy（一种 GPS 性能盒子，专门测 0-100 km/h、0-400m、刹车距离这类加速性能）。商业 Dragy 设备要 2000 元，而一套 ESP32 + MPU6050 + GT-U7 GPS 的料才几十块。

核心挑战不是"读个传感器显示出来"那么简单，而是：

- **传感器装在摩托车上，发动机震动极烈**，MPU6050 原始数据被 20~50 Hz 机械振动淹没。
- **GT-U7 是 30 元级的老 GPS 模块**，静止时速度零漂 ±2 km/h，10 Hz 更新，低速段噪声比信号还大。
- 要在这种"脏数据"上，算出能看的 G 力、倾角、加速测试成绩。

这个项目的真正价值，在于**用软件算法把廉价硬件的潜力榨干**——这也是它后来被整理成面试答辩材料（[INTERVIEW_NOTES.md](INTERVIEW_NOTES.md)）的原因。

### 项目目标

最终想实现一台能：

1. 实时显示**速度 / G 力（纵向、横向、垂直）/ 压弯倾角 / 加速测试成绩**。
2. 自动识别"开始骑行 / 停车"，自动开始/停止数据记录与录音。
3. 把所有数据通过 **WiFi 热点 + 网页**推到手机浏览器（无需 App）。
4. 提供专门的**摩托车模式**：压弯角度、翘头/翘尾检测、弯道半径估算。
5. 数据可落盘（SPIFFS 录音 / SD 卡 CSV 行车数据）。

### 技术栈

| 维度 | 选型 |
|------|------|
| 芯片 | ESP32-WROOM-32（双核 240MHz，板载 WiFi） |
| 框架 | Arduino Framework（基于 ESP-IDF） |
| 操作系统 | 无 RTOS 显式使用，裸机 `loop()` + 软件分时调度（底层是 FreeRTOS，但本项目未直接用任务/队列） |
| 构建系统 | PlatformIO（`platformio.ini`） |
| 语言 | C++（Arduino 风格） + 前端 HTML/CSS/JS（`data/index.html`） |
| 传感器 | MPU6050（6 轴 IMU）、GT-U7（GPS，UBX-M8030 芯片）、模拟麦克风 |
| 显示 | SSD1306 OLED 128×64（I²C） |
| 协议 | I²C（双路）、UART（GPS）、SPI（SD 卡 HSPI）、HTTP（Web）、NMEA/PMTK（GPS 指令） |
| 关键库 | Adafruit MPU6050 / SSD1306 / GFX、TinyGPSPlus、ArduinoJson |
| 机械 | OpenSCAD 参数化外壳（`cad/`） |

### 项目一句话总结（适合放简历）

> 基于 ESP32 + MPU6050 + GT-U7 GPS 自研车载性能仪，通过**三级级联滤波**抑制发动机振动、**GPS-IMU 互补滤波融合**与**动态信心度加权**算法，将 30 元 GPS 模块的静止零漂从 ±2.5 km/h 压到 ±0.8 km/h；实现 0-100 km/h 加速测试、压弯倾角/翘头检测，并通过 WiFi 热点 + 网页实时可视化。

### 项目目录结构分析

```
esp32-imu/
├── platformio.ini              # PlatformIO 工程配置：板型、库依赖、串口
├── src/                        # 固件源码（核心）
│   ├── main.cpp                # 主程序：setup/loop、Web 服务器、OLED 显示、调度
│   ├── mpu6050.{h,cpp}         # IMU 驱动 + 三级振动滤波 + 姿态角计算
│   ├── gps_module.{h,cpp}      # GPS 解析 + 零漂抑制 + GPS-IMU 速度融合 + 性能测试
│   ├── performance_analyzer.{h,cpp} # G 力计算 + 摩托车模式（倾角/弯道/翘头）
│   ├── audio_recorder.{h,cpp}  # ADC 麦克风录音 → WAV，自动骑行触发
│   └── sd_logger.{h,cpp}       # SD 卡 CSV 行车数据记录（HSPI）
├── data/
│   └── index.html              # SPIFFS 网页前端（手机端仪表盘）
├── cad/                        # 机械外壳（OpenSCAD 参数化模型 + STL）
├── interview/                  # LaTeX 版面试材料（main.tex/pdf）
├── INTERVIEW_NOTES.md          # ★ 面试技术答辩归档：踩坑/原理/量化效果（精华）
├── GPS_UPGRADE_ANALYSIS.md     # GPS 硬件升级对比分析
├── GT-U7_ULTIMATE_OPTIMIZATION.md # GT-U7 软件压榨优化记录
├── GPS_WIRING.md / wiring_diagram.txt # 接线图
├── BUILD_GUIDE.md              # 编译/上传/串口/排错指南
└── README.md                   # 原始 README（⚠️ 已与代码脱节，见踩坑记录）
```

> **重点目录提示**：真正的"知识密度"集中在 `src/` 和 `INTERVIEW_NOTES.md`。后者是已经写好的踩坑复盘，本知识库第七部分大量与之呼应。

---

# 第二部分：系统架构设计

## 系统由哪些模块组成

固件按"传感器采集 → 数据融合 → 输出"分层，五个功能模块围绕 `main.cpp` 的调度器协作：

```
┌──────────────────────────────────────────────────────────────┐
│                         main.cpp (调度器)                       │
│   软件分时循环 loop()：按 millis() 时间片驱动各模块             │
└──────────────────────────────────────────────────────────────┘
        │            │             │            │            │
   ┌────▼────┐  ┌────▼────┐   ┌────▼─────┐  ┌───▼────┐  ┌────▼────┐
   │ mpu6050 │  │   gps   │   │ perf_ana │  │ audio  │  │   sd    │
   │  IMU驱动 │  │ GPS解析  │   │ 融合/摩托 │  │  录音  │  │  记录   │
   │ +三级滤波│  │ +零漂抑制 │  │ G力/倾角 │  │ WAV   │  │  CSV    │
   └─────────┘  └─────────┘   └──────────┘  └────────┘  └─────────┘
        │            │             │
        └────────────┴─────────────┘
                     │ 全局数据结构（mpu6050_data / gps_data / fused_data / moto_data）
                     ▼
        ┌────────────────────────────────┐
        │  输出层：OLED显示 + 串口 + Web服务器 │
        └────────────────────────────────┘
```

## 为什么这样拆分模块

- **按物理传感器 + 按算法职责拆分**。`mpu6050`/`gps`/`audio`/`sd` 对应四个外设；`performance_analyzer` 是纯算法层（不碰硬件，只消费别人的全局数据），把"业务逻辑"和"驱动"分开。
- **模块间用全局数据结构通信，而不是函数参数传递**。`mpu6050_data`、`gps_data`、`fused_data`、`moto_data` 都是 `extern` 全局单例。这是嵌入式裸机常见做法：避免动态分配、零拷贝、任何模块随时能读最新值。代价是耦合度高、不可重入——但在单线程 `loop()` 里没有竞态问题。
- **没有用 FreeRTOS 任务**。所有"并发"靠 `loop()` 里基于 `millis()` 的时间片调度实现（见第三部分）。对这种数据流明确、无阻塞操作的场景，裸机分时比多任务更简单、更可控。

## 模块之间如何协作（数据链路）

```
MPU6050 (I²C0, 200Hz)
   │ 原始 a/g/temp
   ▼
[三级滤波] → mpu6050_data.*_Filtered
   │
   ├──────────────► fuseIMUSpeed()  ← GPS速度  ┐
   │                  (互补滤波)               │ 
   ▼                                          ▼
[performance_analyzer]                   gps_data.fused_speed
   calculateGForces() ← GPS速度差分(动态信心度)
   │
   ├─► fused_data (G力/状态/功率)
   └─► moto_data  (倾角/弯道/翘头)
   
GT-U7 GPS (UART2, 10Hz)
   │ NMEA → TinyGPS++ 解析
   ▼
[零漂抑制 三层] → gps_data.speed_kmh / 位置 / 卫星
   │
   ▼
[性能测试状态机] 0-60/0-100/制动测试

输出汇聚：
   gps_data + fused_data + moto_data + audio_data
        │
        ├─► OLED (5种显示模式, 20Hz)
        ├─► 串口 (简要/详细/混合)
        ├─► Web /data /moto /audio (JSON)
        └─► SD卡 CSV (10Hz, 18字段/行)
```

**每条链路的作用**：

| 链路 | 频率 | 作用 |
|------|------|------|
| IMU 采样 → 滤波 | 200 Hz | 高频采集，为 GPS-IMU 融合提供积分基础 |
| IMU → 速度融合 | 200 Hz | 用加速度积分填补 GPS 帧间空白，GPS 帧到来时拉回漂移 |
| GPS → 零漂抑制 | 10 Hz | 把脏速度变成能用的速度，喂给所有下游 |
| 融合 → G力/倾角 | 20 Hz | 算出展示用的 G 力、压弯角、弯道信息 |
| 汇聚 → 输出 | 20 Hz(显示) / 10 Hz(SD) | OLED/网页/串口/落盘 |

---

# 第三部分：项目运行流程

## 上电之后发生什么（初始化流程）

`setup()`（[main.cpp:713](src/main.cpp#L713)）的顺序是经过踩坑调整过的：

```
上电 / Reset
   │
   ▼
Serial.begin(115200)         ← 串口先起，方便看后续日志
   │
   ▼
pinMode(0, INPUT_PULLUP)     ← BOOT 按钮（GPIO0）做显示模式切换
   │
   ▼
双路 I²C 初始化
   I2C_MPU.begin(21,22,400k) ← I2C0 接 MPU6050
   I2C_OLED.begin(18,19,400k)← I2C1 接 OLED
   │
   ▼
scanI2CDevices()             ← 扫总线，确认 0x68/0x3C 在位（排错关键）
   │
   ▼
Init_mpu6050()               ← 设量程 ±8G/±500°/s，DLPF=94Hz，初始化滤波缓冲
   │
   ▼
initGPS()                    ← UART2 9600，发 PMTK 指令配 10Hz + 精简 NMEA + SBAS
   │
   ▼
initPerformanceAnalyzer()    ← 清零 fused_data
initMotorcycleMode()         ← 清零 moto_data，启用互补滤波
initAudioRecorder()          ← 配 ADC1_CH6(GPIO34)，读存储空间
   │
   ▼
[SD 卡初始化被注释跳过]       ← ⚠️ 当前为"测试模式"（见踩坑记录）
   │
   ▼
display.begin()              ← OLED 启动，显示开机画面 2 秒
   │
   ▼
SPIFFS.begin(true)           ← 挂载文件系统（网页 + 录音存这里）
   │
   ▼
WiFi.softAP("ESP32-IMU")     ← 开热点，固定 IP 192.168.4.1
   │
   ▼
server.on(...) + server.begin()  ← 注册 6 个 HTTP 路由
   │
   ▼
display_mode=1, displayQRCode()  ← 初始显示 WiFi 连接信息
```

## 主循环流程（任务调度）

`loop()`（[main.cpp:835](src/main.cpp#L835)）是整个系统的心跳，用 **`millis()` 时间片做软件多任务**，每个"任务"有自己的周期：

```
loop() {
  server.handleClient();        // ① Web 请求：每圈都处理（非阻塞）
  检测 BOOT 按钮 → 切换 display_mode (0~4)
  handleSerialCommands();       // ② 串口命令：每圈都查

  if (now - lastIMUTime >= 5ms)      → ReadMPU6050() + fuseIMUSpeed()   // 200Hz
  if (now - lastGPSTime >= 100ms)    → updateGPSData() + SD记录          // 10Hz
  updateAudioRecorder();             → processSample() + 自动录音检查     // 持续
  if (now - lastUpdateTime >= 50ms)  → updateFusedData() + OLED + 串口   // 20Hz
}
```

时间片分配（这是整个固件的调度核心）：

```
频率   周期    任务                       为什么是这个频率
─────  ─────  ─────────────────────────  ────────────────────────────
200Hz   5ms   IMU采样 + 速度积分融合       足够细的积分步长，抑制积分误差
 20Hz  50ms   数据融合 + OLED + 串口        人眼刷新够用，省CPU
 10Hz 100ms   GPS处理 + SD落盘             GT-U7 硬件就是 10Hz
持续    -     Web / 按钮 / 串口 / 录音采样   尽快响应，无阻塞
```

> **关键设计**：`loop()` 里**绝对没有 `delay()`（除了按钮防抖那一处 200ms）**。所有周期都用"时间戳差值比较"实现非阻塞，这样高频 IMU 不会被低频 GPS 拖慢——这是裸机分时调度能跑起来的前提。

## 中断流程

全项目**只有一处硬件中断**：录音采样定时器。

```
startRecording()
   │
   ▼
timerBegin(0, 80, true)        ← 定时器0，80分频 → 1MHz 计数
timerAlarmWrite(timer, 125)    ← 125 tick = 125µs = 8kHz
timerAttachInterrupt(onSampleTimer)
   │
   ▼ 每 125µs 触发
IRAM_ATTR onSampleTimer() {     ← 中断服务程序（放在 IRAM 加速）
   lastSample = adc1_get_raw()  ← 只做最少的事：读ADC + 置标志位
   sampleReady = true
}
   │
   ▼ loop() 中
processSample() {               ← 真正的处理放在主循环（ISR 外）
   if (!sampleReady) return
   8位量化 + 写缓冲 + 满512字节写SPIFFS
}
```

这是教科书式的 **ISR 最小化**：中断里只读一个 ADC 值、置标志，重活（量化、写 Flash）全部丢回主循环。原因——SPIFFS 写操作可能阻塞，绝不能在 ISR 里做。

## 通信流程

```
GPS:   GT-U7 ──NMEA(9600,UART2)──► gps.encode() 逐字节喂给 TinyGPS++
Web:   手机 ──WiFi──► 192.168.4.1 ──HTTP GET──► /data 返回 JSON ──► 网页轮询
配置:  ESP32 ──PMTK指令(UART)──► GT-U7  (配更新率/NMEA语句/SBAS)
```

## 状态机流程

项目里有**三个独立状态机**：

**1. 录音状态机**（[audio_recorder.cpp:296](src/audio_recorder.cpp#L296)）

```
RECORDING_IDLE/STOPPED ──速度≥5km/h──► RECORDING_ACTIVE
        ▲                                   │
        │                          速度<2km/h 持续5秒
        └───────────────────────────────────┘
   (也可手动 start/stop/pause/resume)
```

**2. 加速/制动测试状态机**（[gps_module.cpp:337](src/gps_module.cpp#L337)）

```
速度跨过 5km/h ──► 启动加速测试计时
   ├─ 跨 60km/h  → 记 0-60 时间
   ├─ 跨 100km/h → 记 0-100 时间
   └─ 跨 200km/h → 记 100-200，结束
速度≥95km/h ──► 启动制动测试 ──速度≤5km/h──► 记 100-0 时间
```

**3. SD 自动记录状态机**（[sd_logger.cpp:280](src/sd_logger.cpp#L280)）：速度 >5 km/h 开始记录，停车 30 秒后结束。逻辑与录音状态机平行。

---

# 第四部分：核心模块解析

## 模块一：MPU6050 IMU 驱动（`mpu6050.cpp`）

**职责**：读 6 轴数据，做振动滤波，算姿态角。

**设计思路**：用 Adafruit 库读原始值，但**不信任原始值**——叠加自己的三级软件滤波（见第五部分振动专题）。这是因为发动机振动让原始数据无法直接用。

**核心数据结构** `MPU6050_DATA`：同时保存"原始值"和"_Filtered 滤波值"两套。下游算 G 力用滤波值，算融合用原始值（积分对延迟敏感）。

**核心函数**：

| 函数 | 作用 |
|------|------|
| `Init_mpu6050()` | 量程 ±8G/±500°/s，DLPF=94Hz；失败时自动尝试 0x69 地址 |
| `ReadMPU6050()` | 读 a/g/temp → 滤波 → 算姿态角 |
| `FilterDataForVibration()` | 滑动平均(10点) + 一阶 IIR(α=0.1) 级联 |
| `CalculateAttitude()` | `atan2` 算 Roll/Pitch（⚠️ Yaw 硬编码为 0） |

**调用关系**：`main.loop()` 每 5ms 调 `ReadMPU6050()` → 内部链式调用滤波和姿态计算。

## 模块二：GPS 模块（`gps_module.cpp`）

**职责**：解析 NMEA、抑制零漂、GPS-IMU 速度融合、跑加速/制动测试状态机。这是**代码量最大、算法最密集的模块（756 行）**。

**设计思路**：GT-U7 数据脏，所以这个模块本质是"**清洗 + 融合**"流水线。配置阶段用 PMTK 指令把 GPS 调到 10Hz 并精简 NMEA（只留 RMC+GGA，否则 9600 波特率会溢出）。

**核心函数**：

| 函数 | 作用 |
|------|------|
| `initGPS()` | UART2 + PMTK 配置（10Hz/精简NMEA/SBAS/WAAS） |
| `updateGPSData()` | 逐字节喂 TinyGPS++ → 三层零漂抑制 → 信号监控自恢复 |
| `fuseIMUSpeed()` | GPS-IMU 互补滤波（核心算法，见第五部分） |
| `calibrateIMUBias()` | 静止时递推均值在线校准加速度偏置（O(1) 内存） |
| `interpolateSpeedAtTime()` | 速度阈值穿越线性插值，把 10Hz 的 100ms 不确定度降到 ~10-20ms |
| `calculateDistance()` | Haversine 球面距离（用于位置跳变检测、圈速） |
| `saveSessionData()/loadSessionData()` | 用 ArduinoJson 把成绩存进 SPIFFS |

## 模块三：性能分析器 + 摩托车模式（`performance_analyzer.cpp`）

**职责**：纯算法层。把 IMU + GPS 融合成 G 力，再衍生出摩托车专属指标。

**设计思路**：分两套数据 `fused_data`（通用 G 力/功率）和 `moto_data`（压弯/翘头/弯道）。

**核心函数**：

| 函数 | 作用 |
|------|------|
| `calculateGForces()` | 动态信心度加权融合 GPS 差分加速度与 IMU；静止/动态两套逻辑 |
| `calculateLeanAngle()` | **互补滤波**算压弯倾角（陀螺仪积分 + 加速度计修正，α=0.96） |
| `detectWheelieStoppie()` | 俯仰角 + 前向 G 力联合判断翘头/翘尾 |
| `analyzeCorner()` | 弯道进出检测 + 半径估算 `r = v²/(g·G_lat)` |
| `estimatePower()` | 用滚阻+风阻+加速力模型估算功率(kW) |

> ⚠️ 注意一个微妙点：`calculateLeanAngle()` 里 `Gyro_Z_Filtered` 被当作"横滚"、`Gyro_X_Filtered` 当作"偏航"，因为**传感器是垂直安装的**（X 轴朝下承重力）。坐标轴映射是按这个安装姿态硬编码的——换安装方向就要改。

## 模块四：录音模块（`audio_recorder.cpp`）

**职责**：定时器中断采集麦克风 ADC，存成 WAV 到 SPIFFS，骑行时自动录。

**核心数据结构**：`WAVHeader`（44 字节标准 WAV 头，先占位后回填 size）、`AUDIO_RECORDER_DATA`（含录音状态机字段）。

**设计思路**：8kHz / 8bit / 单声道——刻意压低规格省 SPIFFS 空间（SPIFFS 才几 MB）。中断只读 ADC，写盘在主循环。

## 模块五：SD 记录（`sd_logger.cpp`）

**职责**：把每个数据点（18 字段）写成 CSV 行到 SD 卡，方便事后用 Excel/Python 分析。

**设计思路**：用 **HSPI**（而非默认 VSPI），MISO 改到 GPIO25 避开 GPIO12 的启动冲突。每 100 个点 flush 一次（平衡掉电安全和写入性能）。

> ⚠️ **当前状态**：SD 初始化在 `setup()` 里被注释掉了（"测试模式"），但 `loop()` 里仍调用 `checkAutoLogging` 和 `logDataPoint`。因为 `sd_status.initialized=false`，这些调用会安全地早退，不写盘。这是一个**半成品功能**。

## 模块六：Web 服务器（在 `main.cpp` 内）

**职责**：WiFi 热点 + HTTP 服务，把数据 JSON 化推给手机网页。

**路由表**：

| 路由 | 作用 |
|------|------|
| `/` | 从 SPIFFS 返回 `index.html`（带 5 分钟缓存头） |
| `/data` | 主数据 JSON：温度/G力/角度/GPS/性能 |
| `/moto` | 摩托车模式 JSON：倾角/弯道/翘头/状态 |
| `/audio` | 录音状态 |
| `/audio/control` | 录音控制（start/stop/pause/list/...） |
| `/audio/download` | 下载 WAV 文件 |

**设计思路**：用 `sprintf` 手动拼 JSON 字符串（而非 ArduinoJson 序列化），固定大小 `char[]` 缓冲。原因——`/data` 是高频轮询接口，手拼 JSON 比构建 JsonDocument 快、内存可控。⚠️ 代价：缓冲区大小要算准，否则溢出（见踩坑记录）。

---

# 第五部分：技术知识提取

> 本部分只讲**项目里真的用到、且有"为什么"的**知识点，不抄教科书定义。

## 知识点 1：I²C（双路总线设计）

**为什么需要它**：MPU6050 和 OLED 都是 I²C 设备，要挂到 ESP32。

**项目中的实际应用**：项目用了**两个独立的 I²C 控制器**（ESP32 有两个硬件 I²C）：`TwoWire(0)` 给 MPU6050（GPIO21/22），`TwoWire(1)` 给 OLED（GPIO18/19）。

**为什么不共享一条总线**：共享其实完全可行（地址 0x68 和 0x3C 不冲突）。分两路的好处是：OLED 刷新（大量数据传输）不会阻塞 IMU 的高频读取，互不干扰。代价是多占两个 GPIO。

**常见误区/坑**：README 和 main.cpp 的注释里都写过"共享总线 GPIO21/22"，但实际代码是双路。**文档和代码已脱节**（见踩坑记录）——以代码为准。

**调试方法**：`scanI2CDevices()` 分别扫两条总线，打印找到的地址。这是 I²C 排错第一招。

## 知识点 2：振动环境下的三级级联滤波（本项目灵魂）

**它解决什么问题**：发动机怠速 20~50 Hz 振动叠加到加速度上，G 力跳变、姿态角抖 ±5°。

**工作原理 — 三级级联**：

```
原始ADC
   │
① 硬件 DLPF (MPU6050内置数字低通)
   setFilterBandwidth(94Hz)        ← 砍掉 >94Hz，但保留快速动作响应
   │
② 滑动平均 FIR (窗口10点 @200Hz)
   等效时间窗50ms，截止≈10Hz        ← 无相位累积，但延迟25ms
   │
③ 一阶 IIR 低通 (α=0.1)
   y=αx+(1-α)y_prev，截止≈3.5Hz     ← O(1)内存，进一步平滑
   │
最终 *_Filtered (等效截止 3~5Hz，振动衰减>12dB)
```

**为什么这么设计（关键权衡）**：

- 为什么 DLPF 选 94Hz 而不是 5Hz？因为陀螺仪要追摩托快速转向（可达 90°/s），截止太低动态响应变迟钝。硬件留宽点，靠软件补。
- 为什么 IIR 不用 FIR？MCU 上 IIR 只存一个上次输出值（O(1)），FIR 要存整个窗口。加速度测量不做频谱分析，对相位延迟不敏感，IIR 划算。
- α=0.1 怎么定的？公式 `fc ≈ α·fs/(2π) ≈ 0.1×200/6.28 ≈ 3.2Hz`，再用串口波形观察微调。

**量化效果**：静止 G 力噪声 ±0.15g → ±0.01g；姿态抖动 ±5° → ±0.5°；代价是 25~50ms 延迟（可接受）。

## 知识点 3：互补滤波（Complementary Filter）

**为什么需要它**：纯加速度计算姿态，在动态（加速/振动）时会把"线加速度"误当"重力"，Pitch 虚假偏 5~10°；纯陀螺仪积分会漂移。

**工作原理**：`θ = α·(θ_prev + ω·dt) + (1-α)·θ_accel`。陀螺仪管高频（快、短期准），加速度计管低频（慢、长期准），互补。

**项目中的实际应用**：摩托车模式的压弯倾角（[performance_analyzer.cpp:524](src/performance_analyzer.cpp#L524)）用 α=0.96 实现。注意**姿态角主路径（mpu6050.cpp）没用互补滤波**，是已知缺口——G 力测量优先级更高，姿态是次要功能。

**常见误区**：α 不是越大越好。α=0.98 时间常数 τ=α·dt/(1-α)≈0.25s，即 0.25s 内加速度计才能把陀螺拉回——这个"拉回速度"要和振动强度匹配着调。

## 知识点 4：GPS-IMU 速度融合 + 动态信心度

**为什么需要它**：GPS 准但慢（10Hz、低速噪声大）；IMU 快（200Hz）但积分会漂。要互补。

**两个融合点**：

1. **速度融合**（`fuseIMUSpeed`，互补滤波）：200Hz 用加速度积分填 GPS 帧间空白，每个 GPS 帧（~100ms）到来时按 `GPS_CORRECTION_RATE=0.1` 拉回 10% 漂移。
2. **G 力融合**（`calculateGForces`，动态信心度）：不是固定权重，而是按实时信号质量算 `imu_confidence = gps稳定性 × 信号质量`，动态分配 GPS 差分加速度和 IMU 的权重。

**为什么不用卡尔曼滤波**：

| | 本项目(互补/自适应IIR) | 卡尔曼(EKF) |
|-|------|------|
| 计算量 | 极低，适合 ESP32 | 矩阵运算重 |
| 调参 | 直观(α、死区) | 要建模 Q/R 协方差 |
| 选择 | ✅ | 改进方向 |

**常见误区**：用 GPS 的速度，**要用 Doppler 测速而非位置差分**。TinyGPS++ 的 `gps.speed.kmph()` 解析的 RMC 速度本身就是 Doppler 速度，精度比位置差分高 ~10 倍。项目用对了。

## 知识点 5：GPS 零漂抑制（三层）

GT-U7 静止时输出 0.5~2 km/h 假速度。三层压制：

```
层1：20点滑动平均 + 标准差分析
     std_dev<0.2 且 avg<动态阈值 → 判定稳定
层2：自适应IIR (按速度段调α)
     <0.5km/h→α=0.92强抑零漂；>4km/h→α=0.35快响应不耽误计时
层3：最终死区
     好信号(≥8星,HDOP<1.0)阈值0.8km/h，否则1.2km/h，以下归零
```

判定静止需持续 1.5 秒（15 次 ×100ms）。这是个**精度 vs 响应**的反复权衡——压太狠会让 0-100 测试起步判断变慢。

## 知识点 6：ADC + 定时器中断采样（录音）

**为什么用 ADC1 的 GPIO34**：ESP32 的 **ADC2 与 WiFi 冲突**（WiFi 开启时 ADC2 不可用）。本项目全程开着 WiFi 热点，所以麦克风必须接 ADC1。GPIO34 是纯输入引脚（无上拉/输出），最适合做模拟输入。这是个**容易踩的硬件坑，项目避开了**。

**采样机制**：硬件定时器 80 分频得 1MHz，125 tick=125µs=8kHz 触发中断。ISR 只 `adc1_get_raw` + 置标志。

## 知识点 7：WAV 文件格式

44 字节 RIFF 头 + PCM 裸数据。录音时先写占位头（size=0），结束时 `seek(0)` 回填真实的 `dataSize`/`fileSize`。8bit 无符号 PCM，中心在 128。

## 知识点 8：SPIFFS vs SD 卡

| | SPIFFS（板载Flash） | SD卡（外接） |
|-|------|------|
| 容量 | ~1-3MB | GB级 |
| 用途 | 网页文件 + 录音(小) | 行车CSV(大量) |
| 接口 | 内部 | SPI(HSPI) |
| 项目状态 | 在用 | 半成品(初始化被注释) |

## 知识点 9：PMTK / NMEA 协议

GPS 用 NMEA 文本协议输出（`$GPRMC`/`$GPGGA`），用 PMTK 专有指令配置。项目发的关键指令：

- `$PMTK220,100*2F`：更新率 100ms（10Hz）
- `$PMTK314,...`：只输出 RMC+GGA（精简，防 9600 波特率溢出）
- `$PMTK313,1*2E`：启用 SBAS
- `$PMTK301,2*2E`：DGPS 用 WAAS

> ⚠️ 注意文档间不一致：`GT-U7_ULTIMATE_OPTIMIZATION.md` 说要把波特率提到 38400、用多星座，但 `initGPS()` 实际代码仍是 9600 + 10Hz。优化文档是"设想"，代码是"现状"。

## 知识点 10：HTTP / WiFi SoftAP

ESP32 开热点（`WiFi.softAP`），固定 IP 192.168.4.1，跑 `WebServer`。网页用 JS 轮询 `/data` 拿 JSON。`/data` 返回 `no-cache`（实时数据），`/`（HTML）返回 `max-age=300`（静态可缓存）。这种缓存策略区分对了。

---

# 第六部分：设计决策分析

### 决策 1：裸机分时调度，而非 FreeRTOS 任务

- **背景**：要"同时"跑 200Hz IMU、10Hz GPS、20Hz 显示、持续录音和 Web。
- **方案**：`loop()` 里用 `millis()` 时间戳差比较做软件分时，全程无 `delay()`。
- **优点**：简单、可控、无竞态（单线程）、无任务切换开销。
- **缺点**：任一环节若阻塞会拖垮全局；CPU 没用上第二个核。
- **替代**：FreeRTOS 多任务 + 队列（ESP32 底层就是 FreeRTOS）。
- **为什么这么选**：数据流明确、无长阻塞操作，裸机分时足够且心智负担小。

### 决策 2：全局数据结构通信，而非消息传递

- **优点**：零拷贝、无动态分配、任意模块读最新值、实现极简。
- **缺点**：强耦合、不可重入、多线程下会有竞态（但本项目单线程，无此问题）。
- **为什么**：嵌入式裸机标准做法，内存确定性 > 解耦优雅。

### 决策 3：环形缓冲区做滑动平均

- **背景**：振动滤波要算窗口均值。
- **方案**：定长数组 + 取模索引 `(idx+1)%N`，配 `buffer_full` 标志处理冷启动。
- **优点**：O(1) 内存、无动态分配、写覆盖最旧。
- **为什么**：MCU 上定长环形缓冲是滑动窗口的标准实现。

### 决策 4：互补滤波而非卡尔曼

见第五部分知识点 4 的对比表。一句话：**ESP32 算力和调参成本下，互补/自适应 IIR 性价比远高于 EKF**，卡尔曼列为改进方向。

### 决策 5：双 I²C 总线

- **优点**：OLED 刷新不阻塞 IMU 读取。
- **缺点**：多占 2 个 GPIO。
- **替代**：单总线共享（地址不冲突，可行）。
- **为什么**：高频 IMU 对总线占用敏感，隔离更稳。

### 决策 6：手拼 JSON 而非序列化库

- **背景**：`/data` 高频轮询。
- **优点**：`sprintf` 比构建 JsonDocument 快、固定 `char[]` 内存可控。
- **缺点**：缓冲区大小要手算，易溢出；可维护性差。
- **替代**：ArduinoJson（项目在低频的 `saveSessionData` 里用了它）。
- **为什么**：热路径选性能，冷路径选可维护性——**两种都用，按场景分**。

### 决策 7：SD 卡用 HSPI + MISO 改 GPIO25

- **背景**：GPIO12 是 ESP32 的 strapping 引脚（启动时拉高会导致 flash 电压判断错误，启动失败）。
- **方案**：SD 用 HSPI（独立于默认 VSPI），并把 MISO 从 GPIO12 挪到 GPIO25。
- **为什么**：避开 strapping 引脚的启动冲突——这是 ESP32 经典硬件坑。

### 决策 8：录音 8kHz/8bit

- **背景**：SPIFFS 只有几 MB。
- **方案**：刻意压低音质规格。8kHz×8bit×单声道 = 8KB/s，5 分钟约 2.4MB。
- **为什么**：录的是"骑行环境音/排气声"，不需要高保真，容量优先。

---

# 第七部分：调试与踩坑记录

> 以下问题部分来自代码痕迹推断，部分已记录在 [INTERVIEW_NOTES.md](INTERVIEW_NOTES.md)。

### 坑 1：IMU 被发动机震动淹没 ★已解决

- **问题**：怠速时 G 力跳变、姿态抖 ±5°。
- **原因**：MPU6050 DLPF 默认 260Hz，等于没滤，20~50Hz 振动全透传。
- **定位**：串口打印原始加速度波形，看到高频毛刺。
- **解决**：三级级联滤波（硬件 94Hz + 滑动平均 + IIR），等效截止 3~5Hz。
- **经验**：传感器原始值不可全信，先看噪声频谱再定滤波策略。

### 坑 2：GPS 静止零漂 ★已解决

- **问题**：停车时显示"车在缓慢移动"，0-100 测试起步判断错。
- **原因**：GT-U7 多路径反射 + 卫星几何误差，静止输出 0.5~2km/h。
- **解决**：三层零漂抑制（滑动平均+标准差 / 自适应IIR / 死区）。
- **经验**：死区阈值要随信号质量动态变，固定阈值要么压不住要么误杀低速。

### 坑 3：IMU 加速度偏置导致积分漂移 ★部分解决

- **问题**：积分求速度漂移大（1m/s² 偏置 ×10s = 10m/s 误差）。
- **原因**：MPU6050 出厂零偏 ±50~150mg。
- **解决**：① 手动测量硬编码 `-0.47/+0.5/-0.48`（performance_analyzer）；② GPS 判静止时递推均值在线校准（gps_module）。
- **遗留**：温漂未补偿；手动硬编码值换硬件就失效。

### 坑 4：GPS 位置跳变 ★已解决

- **问题**：隧道出口/高楼后重定位，位置瞬跳数百米，距离统计飙升。
- **解决**：Haversine 算帧间距离，>1000m（即 >1km/帧，F1 也才 83m/s）直接拒绝。

### 坑 5：GPIO12 启动冲突（SD 卡）★已规避

- **问题**：SD 卡 MISO 默认接 GPIO12 会导致 ESP32 启动失败。
- **原因**：GPIO12 是 strapping 引脚，启动时电平影响 flash 电压选择。
- **解决**：MISO 挪到 GPIO25，用 HSPI。

### 坑 6：ADC2 与 WiFi 冲突（麦克风）★已规避

- **问题**：WiFi 开启时 ADC2 引脚读不到值。
- **解决**：麦克风接 ADC1 的 GPIO34（纯输入引脚）。

### 坑 7：9600 波特率下 10Hz NMEA 溢出 ★已规避

- **问题**：10Hz 全量 NMEA 语句在 9600 波特率下数据溢出、丢帧。
- **解决**：PMTK314 只保留 RMC+GGA 两条语句。

### 坑 8：文档与代码脱节 ⚠️ 待修

- README 写"共享 I²C 总线 GPIO21/19"，实际代码是**双路** GPIO21/22 + GPIO18/19。
- README 的 `lib_deps` 写 `electroniccats/MPU6050`，实际 `platformio.ini` 用 `adafruit/Adafruit MPU6050`。
- `GT-U7_ULTIMATE` 说 38400 波特率/多星座，代码实际 9600/10Hz。
- **经验**：文档要么删要么同步，过时文档比没文档更误导未来的自己。

### 坑 9：SD 记录是半成品 ⚠️ 待完成

- `setup()` 里 `initSDCard()` 被注释（"测试模式"），但 `loop()` 仍调记录函数。
- 因 `initialized=false` 安全早退，不写盘。**功能未真正启用。**

### 潜在风险（代码审查角度，未必触发）

- **JSON 缓冲区**：`/data` 用 `char[750]`、`/moto` 用 `char[600]` 手拼。字段多、`%.8f` 经纬度长，**接近上限，未来加字段易溢出**。建议加 `snprintf` 长度保护。
- **静止校准的 static 局部变量**：`calculateGForces` 里 `gravity_bias_x` 等用 `static` 且只累加不重置，长时间运行逻辑可能僵化（一旦 `calibration_samples` 满 50 就固定）。

---

# 第八部分：工程实践总结

### 做得好的地方

- **代码组织**：按外设/算法清晰分模块，头文件声明 + cpp 实现规整，函数注释到位。
- **接口设计**：全局数据结构单例 + `extern` 声明，模块边界清楚。
- **错误处理**：初始化大量带降级（MPU6050 试 0x68 失败试 0x69；OLED 失败仍继续；GPS 信号丢失自动热重启恢复）。这种"**失败不死机、打印原因、降级运行**"的思路很成熟。
- **可观测性**：串口三种打印模式（简要/详细/混合），可运行时切换；I²C 扫描、原始 NMEA 打印——调试钩子完备。
- **构建系统**：PlatformIO，依赖锁版本（`@^x.y.z`），`monitor_filters = esp32_exception_decoder` 自动解崩溃栈。
- **配置管理**：引脚、阈值、滤波系数都用 `#define`/`const` 集中在文件头，调参不用翻代码。
- **场景化分层**：热路径手拼 JSON 求性能，冷路径用 ArduinoJson 求可维护——分得清。

### 可以改进的地方

- **文档与代码同步**：README/优化文档已严重脱节（坑 8）。
- **魔法数字**：`-0.47/+0.5/-0.48` 这类校准偏置散落在多个文件、重复出现，应抽成统一配置或自动校准。
- **缓冲区安全**：手拼 JSON 用 `sprintf` 无长度保护，应换 `snprintf`。
- **SD 功能收尾**：要么启用要么删，别留半成品调用。
- **测试策略**：目前**无任何自动化测试**（`test/` 只有空 README）。IMU 滤波、Haversine、零漂判定这些纯函数其实很适合做单元测试（host 端跑）。
- **持续集成**：无 CI。可加 PlatformIO 的 `pio ci` 在 push 时编译检查。

---

# 第九部分：项目复盘

### 最大的亮点

把"算法"当成项目主角，而不是"点亮外设"。**三级滤波 + GPS-IMU 融合 + 动态信心度**这套组合，把 30 元 GPS 的静止零漂从 ±2.5 压到 ±0.8 km/h（与专业设备差距从 20 倍缩到 4 倍，成本差 67 倍）。这是真正有技术含量、能讲清"为什么"的部分。

### 最大的难点

**在脏数据上做可信的实时融合**。GPS 慢且低速噪声大，IMU 快但漂，两者都不可单独信任。难点不在写公式，而在：什么时候信谁、信多少（动态信心度）、怎么不让滤波延迟拖垮测试计时（自适应 α）。这些都是反复调参 + 串口看波形磨出来的。

### 学到的知识

- 滤波器选型的工程权衡（FIR vs IIR、硬件 vs 软件、截止频率 vs 响应延迟）。
- 互补滤波 / 传感器融合的实战（而非课本公式）。
- ESP32 硬件陷阱（strapping 引脚、ADC2/WiFi 冲突）。
- 裸机分时调度的设计模式（millis 时间片、ISR 最小化）。
- GPS NMEA/PMTK 协议、Doppler 测速 vs 位置差分。

### 如果重新设计会怎么做

- **传感器融合上 1D 卡尔曼**：只融合纵向速度，状态量少，ESP32 扛得住，比手调信心度更系统。
- **校准全自动化**：删掉所有硬编码偏置，统一走静止在线校准 + 温度补偿。
- **姿态主路径加互补滤波**：解决加速时 Pitch 虚假偏移。
- **抽配置层**：所有引脚/阈值/系数集中到一个 `config.h`。
- **加 host 端单元测试**：纯算法函数脱离硬件测。
- **用 FreeRTOS 双核**：Web/录音放一个核，传感器融合放另一个核，物理隔离。

### 下一步可以扩展什么

- 升级 GPS 到 NEO-M9N（[GPS_UPGRADE_ANALYSIS.md](GPS_UPGRADE_ANALYSIS.md) 已做选型，150 元换 5-8 倍精度）。
- 真正启用 SD 记录 + 配套 Python 分析脚本（CSV 已就绪）。
- 加磁力计（QMC5883L）补全 Yaw / 转向感知。
- 大倾角重力分量补偿：`a_真 = a_测 - g·sin(Roll)`。
- 网页端轨迹回放 / 成绩对比。

---

# 第十部分：个人知识地图

> 由本工作区内容沉淀出的长期知识体系。带 ✅ 的是本项目实际用过的。

```
Embedded Knowledge Map
│
├── MCU 基础 (ESP32)
│   ├── GPIO ✅           (BOOT按钮输入、CS控制)
│   ├── ADC ✅            (ADC1_CH6麦克风；ADC2/WiFi冲突坑)
│   ├── Timer + 中断 ✅    (8kHz采样定时器、IRAM_ATTR、ISR最小化)
│   ├── Strapping引脚 ✅   (GPIO12启动冲突坑)
│   └── 双核 (未用，改进方向)
│
├── 通信协议
│   ├── I²C ✅            (双路总线、设备扫描、400kHz、地址0x68/0x3C)
│   ├── UART ✅           (GPS 9600、HardwareSerial2)
│   ├── SPI ✅            (SD卡 HSPI、避VSPI冲突)
│   ├── HTTP/WiFi ✅      (SoftAP热点、WebServer、JSON、缓存策略)
│   └── NMEA/PMTK ✅      (GPS文本协议、Doppler测速、配置指令)
│
├── 传感器与信号处理 ★本项目核心
│   ├── IMU (MPU6050) ✅  (量程/DLPF配置、6轴)
│   ├── 滤波器 ✅
│   │   ├── 硬件DLPF ✅
│   │   ├── 滑动平均(FIR) ✅
│   │   ├── 一阶IIR/EMA ✅
│   │   └── 卡尔曼 (未用，改进方向)
│   ├── 传感器融合 ✅
│   │   ├── 互补滤波 ✅    (倾角、α=0.96)
│   │   ├── 动态信心度加权 ✅
│   │   └── 阈值穿越插值 ✅
│   ├── 姿态解算 ✅       (atan2算Roll/Pitch；Yaw缺磁力计)
│   └── 偏置校准 ✅       (递推均值O(1)在线校准)
│
├── GPS / 定位
│   ├── NMEA解析 ✅       (TinyGPS++)
│   ├── 零漂抑制 ✅       (三层：均值+自适应IIR+死区)
│   ├── Haversine距离 ✅
│   ├── HDOP/卫星质量 ✅
│   └── SBAS/DGPS增强 ✅
│
├── 数据存储
│   ├── SPIFFS ✅         (网页、录音、会话JSON)
│   ├── SD卡 CSV ⚠️       (半成品)
│   ├── WAV格式 ✅
│   └── ArduinoJson ✅
│
├── 系统架构 / 软件工程
│   ├── 裸机分时调度 ✅    (millis时间片、无delay)
│   ├── 全局单例通信 ✅
│   ├── 环形缓冲区 ✅
│   ├── 状态机 ✅         (录音/测试/SD记录 三个)
│   ├── 错误降级处理 ✅
│   └── RTOS任务/队列 (未用，改进方向)
│
├── 车辆动力学 (应用领域)
│   ├── G力 (纵向/横向/垂直) ✅
│   ├── 压弯倾角 ✅
│   ├── 弯道半径 r=v²/(gG) ✅
│   ├── 翘头/翘尾检测 ✅
│   ├── 加速/制动测试 ✅
│   └── 功率估算(阻力模型) ✅
│
└── 机械 / 工具链
    ├── PlatformIO ✅
    ├── OpenSCAD参数化建模 ✅ (cad/外壳)
    └── Gerber/PCB ✅
```

---

*本知识库由对工作区源码、文档、配置的递归分析生成。凡代码无法确定之处均已标注；未发现编造的功能或设计。若代码变更，请同步更新本文档——过时的笔记比没有笔记更危险。*
